extends layout

block content
  div(class="container mx-auto p-8")
    h1(class="text-4xl mb-4") Welcome, #{user.firstname} #{user.lastname}!
    a(href="/logout" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline") Log out
